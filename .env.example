# DIMENTS GPS Tracker — Jetson .env example
# Let op: dit bestand NIET committen (gebruik .gitignore)

# Server
PORT=3000
DOMAIN=irllogging.duckdns.org

# HTTPS certs (Let's Encrypt via certbot; standaard locatie)
CERT_PATH=/etc/letsencrypt/live/irllogging.duckdns.org

# Data opslag (leeg = ./data in project)
# Tip: later SSD -> DATA_DIR=/mnt/ssd/diments-gps-tracker/data
DATA_DIR=

# Route opslag modus: ndjson (aanrader op Jetson), json (legacy), auto (detect)
ROUTE_STORAGE=ndjson

# Mapbox + Weather
MAPBOX_TOKEN=<<YOUR_MAPBOX_TOKEN>>
OPENWEATHER_KEY=<<YOUR_OPENWEATHER_KEY>>

# Let's Encrypt / certbot
LE_EMAIL=<<YOUR_EMAIL_FOR_LETSENCRYPT>>

# DuckDNS
DUCKDNS_SUBDOMAIN=irllogging
DUCKDNS_TOKEN=<<YOUR_DUCKDNS_TOKEN>>
